<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bio Cosmos - 드레이크 방정식 시뮬레이터</title>
    <style>
        :root {
            --bg-color: #060613;
            --panel-color: #0f0f2d;
            --accent-color: #00ffff;
            --danger-color: #ff0055;
            --text-color: #ffffff;
            --success-color: #00ffaa;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            user-select: none;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            overflow: hidden;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            width: 1200px;
            height: 750px;
            display: grid;
            grid-template-columns: 350px 450px 360px;
            gap: 20px;
            padding: 20px;
            position: relative;
        }

        /* 헤더 및 유틸리티 영역 */
        .top-bar {
            grid-column: span 3;
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 40px;
        }

        .title {
            font-size: 20px;
            font-weight: bold;
            color: var(--accent-color);
            letter-spacing: 1px;
        }

        .utility-btns {
            display: flex;
            gap: 10px;
        }

        .icon-btn {
            background: var(--panel-color);
            border: 1px solid #202060;
            color: var(--text-color);
            width: 40px;
            height: 40px;
            border-radius: 5px;
            cursor: pointer;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 18px;
            transition: all 0.2s;
        }

        .icon-btn:hover {
            border-color: var(--accent-color);
            box-shadow: 0 0 8px rgba(0, 255, 255, 0.3);
        }

        /* 공통 패널 스타일 */
        .panel {
            background-color: var(--panel-color);
            border: 1px solid #1a1a44;
            border-radius: 8px;
            padding: 20px;
            display: flex;
            flex-direction: column;
        }

        .panel-title {
            font-size: 15px;
            font-weight: bold;
            color: var(--accent-color);
            margin-bottom: 20px;
            border-left: 3px solid var(--accent-color);
            padding-left: 8px;
        }

        /* 왼쪽 패널 : 비주얼 & 결과 */
        .visual-panel {
            justify-content: space-between;
        }

        .screen-area {
            background-color: #020208;
            border-radius: 6px;
            height: 200px;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            overflow: hidden;
            border: 1px solid #151535;
        }

        .stars {
            position: absolute;
            width: 100%;
            height: 100%;
            background-image: radial-gradient(white, rgba(255,255,255,.2) 2px, transparent 40px);
            background-size: 50px 50px;
        }

        .life-form-display {
            font-size: 60px;
            z-index: 1;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .result-status {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 6px;
            padding: 15px;
            text-align: center;
            border: 1px dashed #202060;
        }

        .status-tag {
            font-size: 13px;
            font-weight: bold;
            color: #ffaa00;
            margin-bottom: 5px;
        }

        .status-name {
            font-size: 18px;
            font-weight: bold;
            color: var(--text-color);
        }

        /* 중앙 패널 : 슬라이더 및 변수 제어 */
        .control-panel {
            gap: 15px;
            overflow-y: auto;
        }

        .control-group {
            margin-bottom: 5px;
        }

        .control-label-row {
            display: flex;
            justify-content: space-between;
            font-size: 13px;
            margin-bottom: 8px;
        }

        .param-name { color: #a0a0d0; }
        .param-value { color: var(--accent-color); font-weight: bold; }

        input[type="range"] {
            -webkit-appearance: none;
            width: 100%;
            height: 6px;
            background: #151535;
            border-radius: 3px;
            outline: none;
        }

        input[type="range"]::-webkit-slider-thumb {
            -webkit-appearance: none;
            width: 16px;
            height: 16px;
            border-radius: 50%;
            background: var(--accent-color);
            cursor: pointer;
            box-shadow: 0 0 8px var(--accent-color);
        }

        .select-input {
            width: 100%;
            background: #111133;
            border: 1px solid #202060;
            color: var(--text-color);
            padding: 10px;
            border-radius: 5px;
            outline: none;
            cursor: pointer;
        }

        /* 오른쪽 패널 : 힌트 및 세부 조건 */
        .hint-panel {
            gap: 15px;
        }

        .dropdown-row {
            display: flex;
            gap: 10px;
        }

        .hint-box {
            background: rgba(0, 255, 170, 0.05);
            border: 1px solid rgba(0, 255, 170, 0.2);
            border-radius: 6px;
            padding: 15px;
            font-size: 13px;
            line-height: 1.6;
            display: none;
        }

        .hint-box.active {
            display: block;
        }

        .hint-guide-title {
            color: var(--success-color);
            font-weight: bold;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        /* 팝업 모달 (엔딩 북) */
        .modal-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(3, 3, 10, 0.85);
            backdrop-filter: blur(5px);
            z-index: 10;
            display: flex;
            justify-content: center;
            align-items: center;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.3s ease;
        }

        .modal-overlay.active {
            opacity: 1;
            pointer-events: auto;
        }

        .book-container {
            width: 900px;
            height: 600px;
            background: #0b0b22;
            border: 2px solid #202060;
            border-radius: 12px;
            display: grid;
            grid-template-columns: 350px 1px 549px;
            overflow: hidden;
            box-shadow: 0 0 30px rgba(0,0,0,0.6);
        }

        .book-left {
            padding: 25px;
            display: flex;
            flex-direction: column;
            background: #0d0d2a;
        }

        .book-divider {
            background: #202060;
            height: 100%;
        }

        .book-right {
            padding: 30px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            background: #09091c;
        }

        .ending-list {
            list-style: none;
            overflow-y: auto;
            flex-grow: 1;
            margin-top: 15px;
            padding-right: 5px;
        }

        .ending-item {
            padding: 12px;
            border-radius: 6px;
            background: #14143a;
            margin-bottom: 8px;
            cursor: pointer;
            font-size: 13px;
            border: 1px solid transparent;
            display: flex;
            justify-content: space-between;
            align-items: center;
            opacity: 0.5;
            transition: all 0.2s;
        }

        .ending-item.unlocked {
            opacity: 1;
            background: #191950;
        }

        .ending-item:hover {
            border-color: var(--accent-color);
        }

        .ending-item.active {
            border-color: var(--accent-color);
            background: #20206a;
        }

        .status-dot {
            font-size: 11px;
            font-weight: bold;
        }

        .detail-title {
            font-size: 22px;
            font-weight: bold;
            color: var(--accent-color);
            margin-bottom: 15px;
        }

        .detail-story {
            font-size: 14px;
            line-height: 1.7;
            color: #d0d0f0;
            flex-grow: 1;
            background: rgba(0,0,0,0.2);
            padding: 15px;
            border-radius: 6px;
            overflow-y: auto;
            margin-bottom: 20px;
        }

        .detail-conditions {
            background: #111133;
            border: 1px solid #202060;
            padding: 15px;
            border-radius: 6px;
        }

        .condition-tag {
            font-size: 12px;
            color: #aaaaff;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .condition-text {
            font-size: 13px;
            color: var(--success-color);
        }

        .close-book-btn {
            align-self: flex-end;
            background: var(--danger-color);
            color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
            font-size: 13px;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="top-bar">
            <div class="title">BIO COSMOS SIMULATOR</div>
            <div class="utility-btns">
                <button class="icon-btn" id="openBookBtn" title="엔딩 북 열기">📖</button>
                <button class="icon-btn" id="resetBtn" title="시뮬레이터 초기화">🔄</button>
            </div>
        </div>

        <div class="panel visual-panel">
            <div>
                <div class="panel-title">우주생물학 관측 스크린</div>
                <div class="screen-area">
                    <div class="stars"></div>
                    <div class="life-form-display" id="lifeEmoji">🌌</div>
                </div>
            </div>
            
            <div class="result-status">
                <div class="status-tag">[ ⚠️ 관측 완료 ]</div>
                <div class="status-name" id="statusName">데이터 분석 대기 중</div>
            </div>
        </div>

        <div class="panel control-panel">
            <div class="panel-title">물리 및 지질 변수 조절 (ne)</div>
            
            <div class="control-group">
                <div class="control-label-row">
                    <span class="param-name">표면 온도</span>
                    <span class="param-value" id="valTemp">15°C</span>
                </div>
                <input type="range" id="inputTemp" min="-100" max="500" value="15">
            </div>

            <div class="control-group">
                <div class="control-label-row">
                    <span class="param-name">온실가스 농도</span>
                    <span class="param-value" id="valGas">10%</span>
                </div>
                <input type="range" id="inputGas" min="0" max="100" value="10">
            </div>

            <div class="control-group">
                <div class="control-label-row">
                    <span class="param-name">행성 질량비</span>
                    <span class="param-value" id="valMass">1.0 M⊕</span>
                </div>
                <input type="range" id="inputMass" min="1" max="100" value="10" step="1">
            </div>

            <div class="control-group">
                <div class="control-label-row">
                    <span class="param-name">자기장 보호막</span>
                    <span class="param-value" id="valMag">50%</span>
                </div>
                <input type="range" id="inputMag" min="0" max="100" value="50">
            </div>

            <div class="panel-title" style="margin-top: 10px;">화학 및 진화 생태계 변수</div>

            <div class="control-group">
                <div class="control-label-row">
                    <span class="param-name">대기 내 산소 비중</span>
                    <span class="param-value" id="valO2">21%</span>
                </div>
                <input type="range" id="inputO2" min="0" max="100" value="21">
            </div>

            <div class="control-group">
                <div class="control-label-row">
                    <span class="param-name">표면 수분 함량</span>
                    <span class="param-value" id="valWater">70%</span>
                </div>
                <input type="range" id="inputWater" min="0" max="100" value="70">
            </div>

            <div class="control-group">
                <div class="control-label-row">
                    <span class="param-name">우주생물학적 특수 기폭제</span>
                </div>
                <select class="select-input" id="inputCatalyst">
                    <option value="none">없음 (안정된 상태)</option>
                    <option value="radiation">고농도 치사 전리 방사선 구역</option>
                    <option value="asteroid">소행성 상시 충돌대 진입</option>
                </select>
            </div>
        </div>

        <div class="panel hint-panel">
            <div class="panel-title">연구 분석 힌트 모듈</div>
            <div class="dropdown-row">
                <select class="select-input" id="hintSelector">
                    <option value="e1">실패: 폭주한 온실효과의 지옥</option>
                    <option value="e2">실패: 영구 동토의 얼음 행성</option>
                    <option value="e3">실패: 방사선 피폭 황무지</option>
                    <option value="e4">성공: 미생물 군집 행성</option>
                    <option value="e5">성공: 극한 환경의 생명 공학자</option>
                    <option value="e6">성공: 초고도 지적 성간 문명</option>
                </select>
                <button class="icon-btn" id="toggleHintBtn" title="조건 가이드 확인">💡</button>
            </div>

            <div class="hint-box" id="hintBox">
                <div class="hint-guide-title">🎯 조건 범위 가이드:</div>
                <div id="hintContent">조건 로딩 중...</div>
            </div>
        </div>
    </div>

    <div class="modal-overlay" id="bookModal">
        <div class="book-container">
            <div class="book-left">
                <div class="panel-title" style="margin-bottom: 10px;">COSMOS ENDING BOOK</div>
                <ul class="ending-list" id="endingListContainer">
                    </ul>
            </div>
            
            <div class="book-divider"></div>
            
            <div class="book-right">
                <div>
                    <div class="detail-title" id="detailTitle">엔딩을 선택하세요</div>
                    <div class="detail-story" id="detailStory">왼쪽 리스트에서 알고 싶은 환경 분석 결과를 클릭하면 상세 내용과 도달 조건을 확인할 수 있습니다.</div>
                </div>
                <div class="detail-conditions">
                    <div class="condition-tag">🔑 분석 도달 조건 가이드</div>
                    <div class="condition-text" id="detailConditions">엔딩을 선택하면 공식이 나타납니다.</div>
                </div>
                <button class="close-book-btn" id="closeBookBtn">도감 닫기</button>
            </div>
        </div>
    </div>

    <script>
        // --- 엔딩 데이터 정의 ---
        const endings = {
            e1: {
                name: "폭주한 온실효과의 지옥",
                emoji: "🔥",
                story: "수분 함량과 관계없이 온실가스 농도가 지나치게 높아 표면 온도가 급격히 상승했습니다. 물은 전부 증발하고 대기는 두꺼운 열기로 가득 차 지적 생명체는커녕 유기물조차 잔존할 수 없는 죽음의 행성이 되었습니다.",
                conditions: "온실가스 60% 이상 또한 표면 온도 400°C 이상",
                check: (data) => data.gas >= 60 && data.temp >= 400
            },
            e2: {
                name: "영구 동토의 얼음 행성",
                emoji: "❄️",
                story: "항성과의 거리가 너무 멀거나 온실가스 보호막이 부실하여 행성 전체가 얼어붙었습니다. 액체 상태의 물이 존재하지 않아 모든 세포 활동이 동결되었으며, 영구적인 빙하기에 갇히게 되었습니다.",
                conditions: "표면 온도 -50°C 이하",
                check: (data) => data.temp <= -50
            },
            e3: {
                name: "방사선 피폭 황무지",
                emoji: "☢️",
                story: "치사량의 전리 방사선이 쏟아지는 구역 속에서 행성의 자기장 보호막마저 역할을 하지 못했습니다. 행성 표면에 들이닥친 고에너지 입자들이 모든 초기 DNA 구조를 파괴하여 생명의 씨앗이 말라버렸습니다.",
                conditions: "특수 기폭제 [방사선 구역] 입력 상태에서 자기장 보호막 30% 이하",
                check: (data) => data.catalyst === 'radiation' && data.mag <= 30
            },
            e4: {
                name: "미생물 군집 행성",
                emoji: "🦠",
                story: "안정적인 온도와 풍부한 수분을 바탕으로 바다 속에서 원시적인 단세포 유기물들이 탄생했습니다. 비록 지적 생명체나 문명으로 진화하진 못했지만, 행성 전체에 강력한 생명력을 퍼뜨리는 기틀이 마련되었습니다.",
                conditions: "온도 -10°C ~ 40°C 사이, 수분 함량 40% 이상, 특수 기폭제 없음",
                check: (data) => data.temp >= -10 && data.temp <= 40 && data.water >= 40 && data.catalyst === 'none'
            },
            e5: {
                name: "극한 환경의 생명 공학자",
                emoji: "👨‍🔬",
                story: "상시적인 방사선 위협 속에서도 강력한 자기장 보호막을 유지해낸 결과, 방사능을 역이용하여 에너지를 생성하는 극도로 진화한 지적 생명공학 문명이 탄생했습니다. 이들은 유전자 변형에 특화되어 있습니다.",
                conditions: "특수 기폭제 [방사선 구역] 또한 자기장 보호막 70% 이상 그리고 산소 15% 이상",
                check: (data) => data.catalyst === 'radiation' && data.mag >= 70 && data.o2 >= 15
            },
            e6: {
                name: "초고도 지적 성간 문명",
                emoji: "🛸",
                story: "모든 물리, 화학, 생태계 조건이 완벽한 조화를 이루었습니다. 풍부한 산소와 수분을 기반으로 고도화된 지능을 가진 생명체가 출현했으며, 우주 행성 간 통신망을 구축하고 은하계를 탐사하는 지배적인 문명으로 발전했습니다.",
                conditions: "온도 10°C ~ 30°C 사이, 산소 20% ~ 30% 사이, 수분 50% 이상, 자기장 60% 이상",
                check: (data) => data.temp >= 10 && data.temp <= 30 && data.o2 >= 20 && data.o2 <= 30 && data.water >= 50 && data.mag >= 60
            }
        };

        // --- 발견한 엔딩 보관함 (로컬 스토리지 연동) ---
        let unlockedEndings = JSON.parse(localStorage.getItem('cosmos_unlocked_endings')) || [];

        // --- DOM 요소 연결 ---
        const inputTemp = document.getElementById('inputTemp');
        const inputGas = document.getElementById('inputGas');
        const inputMass = document.getElementById('inputMass');
        const inputMag = document.getElementById('inputMag');
        const inputO2 = document.getElementById('inputO2');
        const inputWater = document.getElementById('inputWater');
        const inputCatalyst = document.getElementById('inputCatalyst');

        const valTemp = document.getElementById('valTemp');
        const valGas = document.getElementById('valGas');
        const valMass = document.getElementById('valMass');
        const valMag = document.getElementById('valMag');
        const valO2 = document.getElementById('valO2');
        const valWater = document.getElementById('valWater');

        const lifeEmoji = document.getElementById('lifeEmoji');
        const statusName = document.getElementById('statusName');

        const hintSelector = document.getElementById('hintSelector');
        const hintBox = document.getElementById('hintBox');
        const hintContent = document.getElementById('hintContent');
        const toggleHintBtn = document.getElementById('toggleHintBtn');
        const resetBtn = document.getElementById('resetBtn');

        const bookModal = document.getElementById('bookModal');
        const openBookBtn = document.getElementById('openBookBtn');
        const closeBookBtn = document.getElementById('closeBookBtn');
        const endingListContainer = document.getElementById('endingListContainer');
        
        const detailTitle = document.getElementById('detailTitle');
        const detailStory = document.getElementById('detailStory');
        const detailConditions = document.getElementById('detailConditions');

        // --- 실시간 변수 데이터 획득 ---
        function getSlidersData() {
            return {
                temp: parseInt(inputTemp.value),
                gas: parseInt(inputGas.value),
                mass: parseFloat((inputMass.value / 10).toFixed(1)),
                mag: parseInt(inputMag.value),
                o2: parseInt(inputO2.value),
                water: parseInt(inputWater.value),
                catalyst: inputCatalyst.value
            };
        }

        // --- 수치 라벨 업데이트 ---
        function updateLabels(data) {
            valTemp.innerText = `${data.temp}°C`;
            valGas.innerText = `${data.gas}%`;
            valMass.innerText = `${data.mass} M⊕`;
            valMag.innerText = `${data.mag}%`;
            valO2.innerText = `${data.o2}%`;
            valWater.innerText = `${data.water}%`;
        }

        // --- 엔딩 판정 로직 ---
        function checkSimulation() {
            const data = getSlidersData();
            updateLabels(data);

            let currentEndingKey = null;
            let currentEnding = null;

            // 순차적 판정 (조건 충족 검사)
            if (endings.e1.check(data)) currentEndingKey = 'e1';
            else if (endings.e2.check(data)) currentEndingKey = 'e2';
            else if (endings.e3.check(data)) currentEndingKey = 'e3';
            else if (endings.e5.check(data)) currentEndingKey = 'e5';
            else if (endings.e6.check(data)) currentEndingKey = 'e6';
            else if (endings.e4.check(data)) currentEndingKey = 'e4';

            if (currentEndingKey) {
                currentEnding = endings[currentEndingKey];
                lifeEmoji.innerText = currentEnding.emoji;
                statusName.innerText = currentEnding.name;
                statusName.style.color = currentEndingKey.startsWith('e4') || currentEndingKey.startsWith('e5') || currentEndingKey.startsWith('e6') ? 'var(--success-color)' : 'var(--danger-color)';
                
                // 발견 처리 및 저장
                if (!unlockedEndings.includes(currentEndingKey)) {
                    unlockedEndings.push(currentEndingKey);
                    localStorage.setItem('cosmos_unlocked_endings', JSON.stringify(unlockedEndings));
                }
            } else {
                lifeEmoji.innerText = "⭐";
                statusName.innerText = "단순 유기물 부유 상태 (엔딩 미달)";
                statusName.style.color = var(--text-color);
            }
        }

        // --- 분석 힌트 모듈 제어 ---
        function updateHintModule() {
            const selectedKey = hintSelector.value;
            if (endings[selectedKey]) {
                hintContent.innerText = endings[selectedKey].conditions;
            }
        }

        // --- 엔딩 북 UI 갱신 생성 ---
        function renderEndingBook() {
            endingListContainer.innerHTML = "";
            Object.keys(endings).forEach(key => {
                const isUnlocked = unlockedEndings.includes(key);
                const li = document.createElement('li');
                li.className = `ending-item ${isUnlocked ? 'unlocked' : ''}`;
                li.dataset.key = key;

                li.innerHTML = `
                    <span>${isUnlocked ? endings[key].emoji : '🔒'} ${endings[key].name}</span>
                    <span class="status-dot" style="color: ${isUnlocked ? 'var(--success-color)' : '#7777aa'}">
                        ${isUnlocked ? '● 발견함' : '○ 미발견'}
                    </span>
                `;

                li.addEventListener('click', () => {
                    document.querySelectorAll('.ending-item').forEach(item => item.classList.remove('active'));
                    li.classList.add('active');
                    showEndingDetail(key, isUnlocked);
                });

                endingListContainer.appendChild(li);
            });
        }

        // --- 엔딩 북 상세 정보 채우기 ---
        function showEndingDetail(key, isUnlocked) {
            const ending = endings[key];
            if (isUnlocked) {
                detailTitle.innerText = `${ending.emoji} ${ending.name}`;
                detailStory.innerText = ending.story;
            } else {
                detailTitle.innerText = `🔒 잠겨 있는 미지의 엔딩`;
                detailStory.innerText = "아직 해당 환경 조건을 발견하지 못해 보고서 아카이브가 비어 있습니다. 시뮬레이터 변수를 조절하여 이 환경을 관측하세요!";
            }
            // 조건은 요청에 의해 잠금 여부 상관없이 확인 가능
            detailConditions.innerText = ending.conditions;
        }

        // --- 이벤트 리스너 연결 ---
        const inputs = [inputTemp, inputGas, inputMass, inputMag, inputO2, inputWater, inputCatalyst];
        inputs.forEach(element => {
            element.addEventListener('input', checkSimulation);
            element.addEventListener('change', checkSimulation);
        });

        hintSelector.addEventListener('change', updateHintModule);
        
        toggleHintBtn.addEventListener('click', () => {
            hintBox.classList.toggle('active');
            updateHintModule();
        });

        resetBtn.addEventListener('click', () => {
            inputTemp.value = 15;
            inputGas.value = 10;
            inputMass.value = 10;
            inputMag.value = 50;
            inputO2.value = 21;
            inputWater.value = 70;
            inputCatalyst.value = "none";
            checkSimulation();
        });

        // 모달 제어
        openBookBtn.addEventListener('click', () => {
            renderEndingBook();
            bookModal.classList.add('active');
        });

        closeBookBtn.addEventListener('click', () => {
            bookModal.classList.remove('active');
            // 리셋 상세 정보
            detailTitle.innerText = "엔딩을 선택하세요";
            detailStory.innerText = "왼쪽 리스트에서 알고 싶은 환경 분석 결과를 클릭하면 상세 내용과 도달 조건을 확인할 수 있습니다.";
            detailConditions.innerText = "엔딩을 선택하면 공식이 나타납니다.";
        });

        // 초기 실행
        checkSimulation();
        updateHintModule();
    </script>
</body>
</html>
